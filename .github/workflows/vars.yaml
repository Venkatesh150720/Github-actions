name: vars workflow


on: [push]

env:
    name: ${{ vars.name }}

jobs:
  build_job:
    runs-on: ubuntu-latest
    steps:
        - name: nameof the person
          run: echo"name $name"
          env:
            name: ${{ vars.name }}




