name: vars workflow


on: [push]

env:
    name: ${{ vars.name }}

jobs:
  build_job:
    runs-on: ubuntu-latest
    steps:
        - name: nameof the person
          run: echo "My name is $name"
          env:
            name: ${{ vars.name }}




